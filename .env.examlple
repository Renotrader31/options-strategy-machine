  1	# Options Strategy Generator Environment Variables
     2	
     3	# ENABLE LIVE DATA: Get your free Polygon API key at https://polygon.io/
     4	# Uncomment the line below and add your API key to enable live stock prices:
     5	# POLYGON_API_KEY=your_polygon_api_key_here
     6	
     7	# Without the API key, the app will use realistic mock data
     8	# The sandbox shows "Live Data" but it's actually high-quality mock data
     9	
    10	# Application Configuration
    11	NODE_ENV=development
    12	NEXT_PUBLIC_API_URL=http://localhost:3000/api
